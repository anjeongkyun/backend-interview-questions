# 기여 안내

읽어주셔서 감사합니다. 기여는 네 가지 방식이 있고, **아래로 갈수록 품이 많이 듭니다.** 편한 것부터 하시면 됩니다.

## 1. 받은 면접 질문 제출 (가장 반갑습니다)

면접에서 실제로 받은 질문을 알려주세요. [이슈 열기](../../issues/new?template=received-question.yml)

- 마크다운을 몰라도 됩니다
- **답변을 쓰지 않아도 됩니다.** 질문만으로 충분합니다
- 회사명은 적지 않아도 됩니다. 규모나 직군만 알려주셔도 도움이 됩니다

예상 질문 목록은 어디에나 있지만 **실제로 나온 질문**은 드뭅니다. 이게 이 저장소를 다른 자료와 다르게 만듭니다.

## 2. 오류 신고

사실이 틀렸거나, 오타가 있거나, 표가 깨졌다면 알려주세요. [이슈 열기](../../issues/new?template=error-report.yml)

바로 고치실 수 있으면 PR 도 환영합니다. 오타 수정은 첫 기여로 좋습니다.

## 3. 흔한 실수 추가

이미 있는 답변에 **"이렇게 답하면 틀린다"** 를 보태주세요.

본인이 면접에서 틀렸던 방식이면 가장 좋습니다. 실패 경험이 다른 사람에게는 가장 유용한 정보입니다.

## 4. 답변 작성

답변이 없는 질문에 답변을 써주세요. `answer-needed` 라벨이 붙은 이슈를 보시면 됩니다.

**쓰면서 공부하는 게 목적이라면 이게 가장 효과가 큽니다.** 남에게 설명할 수 있어야 아는 것입니다.

형식은 [STYLE.md](STYLE.md) 를 따라주세요. 처음이라 형식이 어려우면 초안만 주셔도 됩니다. 함께 다듬겠습니다.

---

## PR 을 보내는 방법

```bash
git clone https://github.com/<your-id>/backend-interview-questions.git
cd backend-interview-questions
git checkout -b add-tcp-flow-control-answer
# questions/ 아래 파일을 고치거나 추가
git commit -m "TCP 흐름 제어 답변 추가"
git push origin add-tcp-flow-control-answer
```

파일 위치는 `questions/<토픽>/<개념>.md` 입니다. 새 개념이면 파일을 새로 만드세요.

PR 을 올리기 전에 형식 검사를 돌려보시면 좋습니다.

```bash
python3 scripts/check_format.py
```

## 리뷰에서 보는 것

| 항목 | 기준 |
|------|------|
| 사실 | 틀린 내용이 없는지. 근거가 필요하면 함께 적어주세요 |
| 형식 | [STYLE.md](STYLE.md) 의 골격을 지키는지 |
| 흔한 실수 | 있는지. 없으면 답변이 절반입니다 |
| 문장 | 담백하게. 아래 금지 항목 참고 |

### 문장에서 피하는 것

| 피할 것 | 대신 |
|---------|------|
| em dash(—), 중간점(·) | 쉼표나 문장 나누기 |
| ✅ ❌ ⚠ 같은 상태 기호 | 그 열의 뜻에 맞는 말("가능/불가능", "필요/불필요") |
| 코드블록에 한글 열 맞추기 | 마크다운 표. 한글 고정폭 글꼴이 없어 화면에서 어긋납니다 |
| "매우", "정말", "굉장히" | 없어도 뜻이 같으면 뺍니다 |

마지막 두 개는 실제로 겪은 문제입니다. 코드블록에 공백으로 열을 맞추면 브라우저에서 반드시 깨집니다.

---

## 기여한 내용은 어디에 쓰이나

정직하게 말씀드립니다.

- **이 저장소에 반영됩니다.** PR 이 병합되면 바로 공개됩니다
- **[learn-foundry.app](https://learn-foundry.app) 의 학습 콘텐츠로 반영될 수 있습니다.** 자동은 아니고, 선별해서 문제와 해설로 다듬어 넣습니다. 반영되면 커밋과 크레딧에 남깁니다

두 곳은 **각자 관리됩니다.** 저장소는 읽는 사람을 위해 더 자세히 쓰고, 서비스는 문제로 풀기 좋게 다듬습니다. 그래서 양쪽 문장이 다를 수 있습니다.

라이선스는 [CC BY-SA 4.0](LICENSE) 입니다. 기여하시면 같은 라이선스로 배포되는 것에 동의하는 것으로 봅니다.

## 행동 규범

면접 준비는 불안한 시기에 하는 일입니다. 서로에게 너그럽게 대해주세요.

- 모르는 것을 묻는 것은 환영입니다
- 틀린 답변을 지적할 때는 근거를 함께 주세요
- 특정 회사나 사람을 비방하는 내용은 병합하지 않습니다
