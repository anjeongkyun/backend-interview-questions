# 답변 형식

모든 답변이 같은 골격을 지킵니다. 형식이 일정하면 읽는 사람이 어디를 봐야 할지 압니다.

## 골격

```markdown
### 질문 문장 그대로

한두 문장으로 정의. 결론을 먼저 씁니다.

| 항목 | A | B |
|------|---|---|
| ... | ... | ... |

무엇을 보고 고르는지. 실무 판단 기준.

**흔한 실수:** 이렇게 답하면 틀린다. 왜 틀린지까지.
```

## 각 부분에 무엇을 쓰나

### 정의

**결론을 먼저.** 배경 설명으로 시작하지 않습니다.

```markdown
좋음:  잠금을 걸지 않고 버전으로 충돌을 감지하는 방식입니다.
나쁨:  동시성 제어에는 여러 방법이 있습니다. 그중 하나가...
```

### 비교표

면접 답변의 대부분은 "무엇과 무엇의 차이"입니다. 표로 쓰면 읽는 사람이 축을 바로 봅니다.

**코드블록에 공백으로 열을 맞추지 마세요.** 한글 고정폭 글꼴이 없어 브라우저에서 반드시 어긋납니다. 표를 쓰세요.

```markdown
나쁨:
```
읽기   캐시 먼저
쓰기   DB 먼저
```

좋음:
| 동작 | 순서 |
|------|------|
| 읽기 | 캐시 먼저 |
| 쓰기 | DB 먼저 |
```

### 판단 기준

"실무에서 무엇을 보고 정하나"를 씁니다. 이게 있으면 꼬리 질문에 답할 수 있습니다.

```markdown
기준은 쓰기 충돌 빈도입니다. 충돌이 드물면 낙관적 락이 처리량에서 유리하고,
잦으면 재시도 비용이 잠금 대기보다 커집니다.
```

### 흔한 실수 (필수)

**이 항목이 없으면 답변이 절반입니다.** 그럴듯하지만 틀린 답을 짚어줍니다.

좋은 흔한 실수의 조건은 둘입니다.

1. 실제로 사람들이 그렇게 답한다
2. 왜 틀린지 설명이 붙는다

```markdown
좋음:  **흔한 실수:** 낙관적 락이 항상 빠르다고 답하는 것. 충돌이 잦으면
       재시도가 반복되어 잠금 대기보다 느려집니다.

나쁨:  **흔한 실수:** 개념을 정확히 이해하지 못하는 것.
```

두 번째는 아무 정보가 없습니다.

## 문서 구조

파일 하나가 개념 하나입니다. 그 안에 질문 여러 개가 들어갑니다.

```markdown
# TCP 흐름 제어

> 받는 쪽이 감당할 수 있는 속도로 보내게 조절하는 것

## 이 개념을 왜 묻나

면접관이 확인하려는 것. 한두 문장.

## 질문

### 흐름 제어와 혼잡 제어의 차이는 무엇인가요?
(답변)

### 슬라이딩 윈도우는 어떻게 동작하나요?
(답변)

## 연습 문제

[learn-foundry.app 에서 이 개념 문제 풀기](https://learn-foundry.app/guides/network/tcp-flow-control)

## 관련 개념

- [TCP 3-way Handshake](tcp-3way-handshake.md)
```

## 문장

| 피할 것 | 이유 |
|---------|------|
| em dash(—), 중간점(·) | 한국어 문서에서 어색하고 AI 가 쓴 티가 난다 |
| ✅ ❌ ⚠ ⭐ | 플랫폼마다 모양이 다르고, 같은 기호가 표마다 다른 뜻이 된다 |
| "매우", "정말", "굉장히" | 없어도 뜻이 같다 |
| "~할 수 있습니다" 반복 | 단정할 수 있으면 단정한다 |

숫자는 근거가 있을 때만 씁니다. "훨씬 빠르다" 보다 "수백 배 느리다(비대칭 암호)" 가 낫고, 근거가 없으면 숫자를 쓰지 않습니다.

## 검사

PR 전에 형식 검사를 돌려주세요.

```bash
python3 scripts/check_format.py
```

금지 문자, 코드블록 한글 열 정렬, 표 문법, 흔한 실수 누락을 봅니다.
